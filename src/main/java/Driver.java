public class Driver {
    public static void main(String[] args) {
        //Node<Map<String, Object>> root = createTree();
        //printTree(root, " ");
    }

   /* private static Node<Map<String, Object>> createTree() {

        Node<String, Object> root = new Node<>("root",);


        return root;
    }*/

    /*private static <T> void printTree(Node<T, O> node, String appender) {
        System.out.println(appender + node.getData());
        node.getChildren().forEach(each ->  printTree(each, appender + appender));
    }*/
}
